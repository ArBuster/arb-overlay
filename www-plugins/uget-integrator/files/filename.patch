--- a/bin/uget-integrator
+++ b/bin/uget-integrator
@@ -75,7 +75,8 @@
     elif 'www.youtube.com/watch' in url:
         fileName = ''
     else:
-        fileName = basename(urlparse(url).path).replace('%20', ' ')
+        # fileName = basename(urlparse(url).path).replace('%20', ' ')
+        fileName = ''
 
     return fileName
 

