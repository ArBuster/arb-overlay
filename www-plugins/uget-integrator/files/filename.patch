--- /uget-integrator-1.0.0/bin/uget-integrator
+++ /uget-integrator-1.0.0/bin/uget-integrator
@@ -75,7 +75,8 @@
     elif 'www.youtube.com/watch' in url:
         fileName = ''
     else:
-        fileName = basename(urlparse(url).path).replace('%20', ' ')
+        # fileName = basename(urlparse(url).path).replace('%20', ' ')
+        fileName = ''
 
     return fileName
 

